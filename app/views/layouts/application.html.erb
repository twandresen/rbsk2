<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="robots" content="noindex, follow">
    <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black">
    <meta name="format-detection" content="telephone=no">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="author" content="">

    <%= favicon_link_tag asset_path('favicon.ico'), :rel => 'icon', :type => "image/x-icon" %>

    <link rel="apple-touch-startup-image" href="<%= asset_path('favicon.ico') %>" media="(orientation:landscape)" />
    <link rel="apple-touch-startup-image"  href="<%= asset_path('favicon.ico') %>" media="(orientation:portrait)" />
    <link rel="apple-touch-icon"  href="<%= asset_path('favicon.ico') %>" />

    <%= csrf_meta_tags %>

    <title><%= content_for?(:browser_title) ? yield(:browser_title) + " | " : "" %>RBSK Law</title>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

		<!--link rel="stylesheet" media="screen" href="css.css" /-->

  </head>

  <body class="<%= body_classes %>">

		<%= render partial: "shared/nav" %>

		<%= render partial: "shared/flash"%>

    <%= yield %>

  	<%= yield :scripts %>

		<%= render partial: "shared/footer" %>

  </body>
</html>
